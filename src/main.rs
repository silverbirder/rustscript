extern crate rustscript;

use rustscript::*;

fn main() {
    hello_world::hello_world();
    // sum::sum();
    // fizz_buzz::fizz_buzz();
    // ecma_parser::ecma_parser();
    // ecma_parser_and_llvm::ecma_parser_and_llvm();
}